[phases.install]
# Install frontend first, then backend
install = """
cd frontend
npm install
cd ../server
npm install
"""

[phases.build]
# Build frontend, backend is JS so no build needed
build = """
cd frontend
npm run build
"""

[phases.start]
# Start the backend
start = """
cd server
npm start
"""
